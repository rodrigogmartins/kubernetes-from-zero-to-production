apiVersion: v1
kind: Service
metadata:
  name: fizzbuzz
  namespace: 03-ingress
spec:
  selector:
    app: fizzbuzz-k8s
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP