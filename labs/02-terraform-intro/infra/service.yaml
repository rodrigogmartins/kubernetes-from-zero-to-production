apiVersion: v1
kind: Service
metadata:
  name: terraform-intro
  namespace: lab-02
spec:
  selector:
    app: terraform-intro
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer