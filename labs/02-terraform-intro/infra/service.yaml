apiVersion: v1
kind: Service
metadata:
  name: terraform-intro
  namespace: 02-terraform-intro
spec:
  selector:
    app: terraform-intro
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer