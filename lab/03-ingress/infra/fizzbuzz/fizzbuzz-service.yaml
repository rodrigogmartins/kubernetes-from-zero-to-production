apiVersion: v1
kind: Service
metadata:
  name: fizzbuzz
  namespace: lab-03
spec:
  selector:
    app: fizzbuzz-k8s
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP